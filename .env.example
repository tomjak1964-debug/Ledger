# Placeholders for the planned Supabase backend (see the migration plan in CLAUDE.md).
# Not used by the current single-file app. Copy to .env and fill in when you start the backend.
# .env is gitignored; this example file is tracked.

SUPABASE_URL=your-project-url
SUPABASE_ANON_KEY=your-anon-key
