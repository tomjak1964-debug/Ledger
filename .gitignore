# Dependencies (once a build step / Supabase is added)
node_modules/
.pnp/
.pnp.js

# Build output
dist/
build/
.vite/
.cache/

# Environment / secrets (keep .env.example tracked)
.env
.env.local
.env.*.local
!.env.example

# App data backups exported from Settings
ledger-backup-*.json

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo

# OS cruft
.DS_Store
.DS_Store?
._*
Thumbs.db
ehthumbs.db
Desktop.ini

# Never commit credentials
Keys and Passwords/

# Business data exports for migration - never commit
Sage Export/
Examples/

# Capacitor: generated web assets copied by `cap sync` (regenerate, don't commit)
app/android/app/src/main/assets/public/
app/android/app/src/main/assets/capacitor.config.json
app/android/app/src/main/res/xml/config.xml

# Local data imports/backups — never commit (contains business data)
Import/
